# queso
program quesito:

main()
{
	var
	int A, B, C, D;

	if (A + B > C * D)
	{
		A = B + D
	}
	else
	{
		A = D - C
	}
	B = A * C + D

	#A = B * (C / (D + E * F) * G + H - I (J + K) - M ) + P;

	#if (A + B > C * D)
	#{
	#	A = B + C;
	#}
	#else
	#{
	#	B = C - D
	#}
	#D = A + B;
}